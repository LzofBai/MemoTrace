# Cheat Engine 快速查找密钥指南

## 为什么用 Cheat Engine？

- ✅ 图形界面，操作直观
- ✅ 搜索功能强大
- ✅ 内存浏览器易于查看
- ✅ 成功率高

---

## 5 分钟快速开始

### 步骤 1: 下载（30 秒）

访问：https://www.cheatengine.org/

下载并安装（安装时注意取消勾选不需要的软件）

### 步骤 2: 打开并附加（30 秒）

1. 打开 Cheat Engine
2. 点击左上角的 **电脑图标** 🖥️
3. 找到并选择 `Weixin.exe`
4. 点击 **Open**

### 步骤 3: 搜索手机号（1 分钟）

在 Cheat Engine 主界面：

| 设置项 | 值 |
|--------|-----|
| **Value Type** | String |
| **Text** | `18206740264` |
| 勾选 **Unicode** | ☑️ |
| **Scan Type** | Exact Value |

点击 **First Scan**

等待扫描完成...

### 步骤 4: 查看结果（2 分钟）

1. 在左侧结果列表中，右键点击第一个地址
2. 选择 **"Browse this memory region"**
3. 内存浏览器窗口打开

### 步骤 5: 识别密钥（1 分钟）

在内存浏览器中：

1. **找到手机号显示**
   - 滚动查找 `18206740264`
   - 显示为：`1.8.2.0.6.7.4.0.2.6.4.`（点代表 00）

2. **在手机号附近寻找密钥**
   - 向上滚动约 100-500 字节
   - 向下滚动约 100-500 字节
   - 寻找 **32 字节** 的随机数据

3. **密钥特征**：
   ```
   ❌ 不是密钥：61 62 63 64 ...（可打印字符 abc...）
   ✅ 可能是密钥：A1 B2 C3 D4 E5 F6 ...（随机值）
   ```

### 步骤 6: 复制密钥（30 秒）

1. 选中 32 字节数据（64 个十六进制字符）
2. 右键 -> **Copy** -> **Hex** ( NOT "Hex string" )
3. 粘贴到记事本

示例输出：
```
a1b2c3d4e5f6789012345678abcdef00112233445566778899aabbccddeeff
```

### 步骤 7: 验证密钥（30 秒）

```bash
python manual_key_helper.py
# 粘贴密钥进行验证
```

---

## 图片示例说明

### 内存浏览器界面

```
地址         十六进制数据                      ASCII
00400000  | 4A 55 FF 67 70 5A 00 00 ... |  JU.gpZ...  <- 可能是昵称
00400010  | 11 22 33 44 55 66 77 88 ... |  ."3DUfw...  <- 可能是密钥！
00400020  | 31 00 38 00 32 00 30 00 ... |  1.8.2.0...  <- 手机号
```

**关键观察**：
- 密钥在手机号上方约 16 字节处
- 密钥数据看起来是随机乱码
- 长度正好是 32 字节

---

## 常见错误

### 错误 1: 找不到手机号

**解决**：
- 确认微信已登录（能看到聊天界面）
- 尝试搜索 `0264`（后4位）
- 尝试搜索 `182`（前3位）

### 错误 2: 找到太多结果

**解决**：
- 使用完整的 11 位手机号
- 确保勾选了 **Unicode**

### 错误 3: 不知道哪个是密钥

**解决**：
- 收集 3-5 个候选
- 逐一用 `manual_key_helper.py` 验证
- 正确的密钥会显示 "SUCCESS"

---

## 一键验证脚本

创建 `test_key.bat`：
```batch
@echo off
python manual_key_helper.py
pause
```

双击运行即可。

---

## 成功标志

当 `manual_key_helper.py` 显示：
```
============================================================
SUCCESS! Key is valid!
============================================================
Key: a1b2c3d4...
============================================================
```

✅ **成功！** 现在可以解密数据库了。

---

## 下一步

1. 复制密钥
2. 编辑 `1-decrypt-fixed.py`
3. 粘贴到 `MANUAL_KEY_V4 = ""`
4. 运行 `python 1-decrypt-fixed.py`
5. 选择模式 2

---

**整个过程预计 5-10 分钟，祝你好运！** 🍀
