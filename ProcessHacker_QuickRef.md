# Process Hacker 快速参考卡

## 🔍 搜索步骤速查

### 1. 打开和准备
```
1. 以管理员身份运行 System Informer
2. 找到 Weixin.exe 进程
3. 右键 -> Miscellaneous -> Find strings
```

### 2. 搜索手机号
```
搜索类型: Unicode (UTF-16LE)
搜索内容: 18206740264
搜索范围: All memory regions
```

### 3. 十六进制搜索（备用）
```
搜索类型: Bytes/Hex
搜索内容: 31 00 38 00 32 00 30 00 36 00 37 00 34 00 30 00 32 00 36 00 34 00
```

### 4. 查找密钥
```
在找到的手机号地址附近 (-512 ~ +512 字节)
寻找 32 字节随机数据
特征: 64 个十六进制字符，高随机性
```

---

## 📝 数据格式速查

### UTF-16LE 编码转换

| 字符 | UTF-16LE 字节 |
|------|--------------|
| 0-9 | `30-39` + `00` |
| A-Z | `41-5A` + `00` |
| a-z | `61-7A` + `00` |
| 汉字 | 两个字节（小端序） |

### 示例

**手机号 18206740264**
```
31 00 38 00 32 00 30 00 36 00 37 00 34 00 30 00 32 00 36 00 34 00
```

**昵称 "啊伟"**
```
4A 55 FF 67 70 5A
```

---

## 🎯 密钥特征

### 有效密钥的特征
- ✅ 长度: 32 字节 (64 十六进制字符)
- ✅ 随机性: 包含各种字节值
- ✅ 位置: 通常在用户信息附近
- ✅ 前缀: 可能有 `00 00 00 00 00 00 00 20 00 00 00 00 00 00 00 2F`

### 无效的候选
- ❌ 全是可打印字符 (ASCII 32-126)
- ❌ 大量重复字节
- ❌ 明显的字符串 (如路径、URL)
- ❌ 全零或全相同字节

---

## ⚡ 常用快捷键

| 操作 | 快捷键 |
|------|--------|
| 查找字符串 | Ctrl + Shift + S |
| 内存编辑器 | 双击地址 |
| 复制十六进制 | Ctrl + Shift + C |
| 刷新进程列表 | F5 |

---

## 🔧 验证流程

```
1. 在 Process Hacker 找到候选密钥
2. 复制 32 字节数据的十六进制值
3. 运行: python verify_key_manual.py
4. 粘贴密钥进行验证
5. 如果失败，返回步骤 1 尝试其他候选
```

---

## 🚨 常见问题

### 找不到手机号？
- 确保微信已完全登录
- 尝试搜索部分号码（如 `0264`）
- 检查是否使用了正确的编码（UTF-16LE）

### 内存显示 ???
- 必须以管理员身份运行
- 某些内存页受保护，无法读取

### 找到多个候选？
- 使用验证脚本逐一测试
- 优先测试可读写的内存区域
- 优先测试与 Weixin.dll 相关的地址

---

## 📊 内存区域类型

| Type | 说明 | 是否搜索 |
|------|------|---------|
| Private | 私有内存 | ✅ 是 |
| Mapped | 映射文件 | ⚠️ 可选 |
| Image | 可执行映像 | ❌ 否 |

---

## 💾 保存结果

找到有效密钥后：
```
1. 复制完整的 64 字符十六进制字符串
2. 保存到安全的地方
3. 运行: python 1-decrypt-fixed.py
4. 填入密钥进行解密
```

---

**提示**: 如果 Process Hacker 搜索困难，可以尝试使用 `fix_db_key_v4.py` 自动扫描。
